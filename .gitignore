# Пушим ТОЛЬКО исходный код плагина (папка src) — всё остальное в корне
# (pom.xml, README, AGENTS.md, references/, кэши тестового сервера и т.п.)
# в этот репозиторий сознательно не публикуется.
/*
!/src/
!/.gitignore

# Тестовые классы в публичный репозиторий не выкладываем — остаются только локально
/src/test/
